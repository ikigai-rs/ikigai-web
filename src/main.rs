//! The `ikigai-web` server binary. The face lands in the first PR; the
//! skeleton refuses to pretend it serves anything yet.

fn main() {
    eprintln!("ikigai-web: the HTTP face is not built yet (repo skeleton)");
    std::process::exit(1);
}
